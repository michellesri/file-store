const assert = require('assert');
const fs = require('fs');
const tvShows = require('../lib/tvShows');

describe('get my shows', () => {
  it('gets all tv shows', (done) => {
    console.log('tests is working');
    done();
  });
});
